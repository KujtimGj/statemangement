import 'package:flutter/material.dart';


class GetXDart extends StatelessWidget {
  const GetXDart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(
        centerTitle: true,
        title:const Text("GetX"),
        backgroundColor: Colors.redAccent,
      ),
    );
  }
}
